import * as C from './styles';

type Props = {
  x: number;
  y:number;
}

const Pokemon = ({x, y} : Props) => {
  const size = 50;

    return(
      <C.Container>
          
      </C.Container>  
    );
}

export default Pokemon;